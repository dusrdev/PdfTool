<Window x:Class="ImageToPdf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageToPdf"
        mc:Ignorable="d"
        Title="ImageToPdf - By David Shnayder"
        ResizeMode="NoResize"
        Height="540" Width="540">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border x:Name="BorderDrop" Margin="5" CornerRadius="5" Background="LightGray" Grid.Row="0" DragDrop.Drop="BorderDrop_Drop"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0"/>
            <Button Content="Save" Grid.Column="1"/>
        </Grid>
    </Grid>
</Window>
